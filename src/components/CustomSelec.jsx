import React from 'react';

function CustomSelec({ options, onSelect }) {
    return (
        <div style={{position: 'relative'}}>
            <div className="select">
                <span>Select</span>
            </div>
            <div className="options_list">
                
            </div>
        </div>
    );
}

export default CustomSelec;